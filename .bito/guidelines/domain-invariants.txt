Critical invariants for contentful/laravel:

1. ALWAYS call `$client->useIntegration($this)` after constructing the Client singleton in ContentfulServiceProvider::register(). Omitting this silently breaks Contentful SDK telemetry.

2. NEVER commit composer.lock. This is a library package. Lock files are for applications, not libraries. Committing one will break downstream consumers' dependency resolution.

3. Config keys under `contentful.delivery.*` are part of the public API. Do not rename, remove, or restructure them in a minor or patch release. Only change them in a major version bump with a CHANGELOG entry.

4. The `delivery.options` config value must support either a callable (fn(ClientOptions, Application): void) or an empty array ([]). Do not narrow this to only callable without a major bump.

5. ContentfulServiceProvider MUST remain a deferred provider (`$defer = true`). Do not make it eager without a strong justification — lazy-loading the Contentful client is intentional behavior.

6. The `provides()` method MUST return `[Client::class]` exactly. This is what Laravel uses to know when to instantiate the deferred provider.

7. The `ContentfulDelivery` facade accessor MUST return `Client::class`. This must match what `register()` binds in the container.

8. Test credentials (`cfexampleapi` / `b4c0n73n7fu1`) in phpunit.xml.dist are intentionally public and safe to commit. They belong to Contentful's public example space. Do not replace them with real credentials.

9. PHP version support: follow Contentful's policy of dropping PHP versions at EOL. Do NOT add back support for dropped versions. Major version bumps are required when dropping PHP floor.

10. Laravel version support: follow the same pattern — add support for new major Laravel versions as they release; drop only via major bump.

<!-- Generated by seed-golden-context | Last updated: 2026-05-11 -->
